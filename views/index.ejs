<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="css/style.css" />
    <script
      src="https://kit.fontawesome.com/3bfd3ce9e5.js"
      crossorigin="anonymous"
    ></script>
    <title>Albums of the Year</title>
  </head>
  <body>
    <h1>Top Albums</h1>
    <ul class="albums">
      <% for(let i = 0; i < info.length; i++){%>
      <li class="album">
        <span><%= info[i].albumname %></span>
        <span><%= info[i].artistname %></span>
        <span><img src="<%= info[i].photo %>" /></span>
        <span><%= info[i].likes %></span>
        <span class="thumbs-up" id="thumbsUp"><i class="fa-solid fa-thumbs-up"></i></span>
        <span class="trash" id="trashCan"><i class="fa-solid fa-trash-can"></i></span>
      </li>
      <% } %>
    </ul>

    <h2>Add an album, artist, and photo!</h2>

    <form action="/addAlbum" method="POST">
      <input type="text" placeholder="album title..." name="albumname" />
      <input type="text" placeholder="artist..." name="artistname" />
      <input type="url" placeholder="add photo URL" name="photo" />
      <input type="submit" />
    </form>

    <script src="js/main.js"></script>
  </body>
</html>
